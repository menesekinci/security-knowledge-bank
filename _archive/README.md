# Security Knowledge Bank — Archive

## legacy-chunks/

Older intermediate chunk set (**334** files) produced before the full embedding pipeline.

**Canonical set:** `../embeddings/chunks/` (**1002** files + `manifest.json`).

Do not use `legacy-chunks` for RAG ingestion unless you have a specific migration reason.
